import * as React from 'react';
import { PopupPadding } from '@workday/canvas-kit-react-popup';
import { ModalWidth } from './Modal';
export interface ModalContentProps extends React.HTMLAttributes<HTMLDivElement> {
    padding: PopupPadding;
    width: ModalWidth;
    handleClose?: () => void;
    closeOnEscape: boolean;
    heading: React.ReactNode;
    firstFocusRef?: React.RefObject<HTMLElement>;
}
declare const ModalContent: ({ handleClose, children, closeOnEscape, firstFocusRef, width, heading, padding, ...elemProps }: ModalContentProps) => JSX.Element;
export default ModalContent;
