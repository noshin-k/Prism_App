import { PopupPadding } from '@workday/canvas-kit-react-popup';
import { ModalContentProps } from './ModalContent';
export declare enum ModalWidth {
    s = "440px",
    m = "800px"
}
export interface ModalProps extends ModalContentProps {
    open: boolean;
}
declare const Modal: {
    ({ open, ...modalContentProps }: ModalProps): JSX.Element | null;
    Padding: typeof PopupPadding;
    Width: typeof ModalWidth;
    defaultProps: {
        open: boolean;
        padding: PopupPadding;
        width: ModalWidth;
        closeOnEscape: boolean;
    };
};
export default Modal;
export declare function useModal(): {
    targetProps: {
        onClick(): void;
    };
    closeModal(): void;
    modalProps: {
        open: boolean;
        handleClose(): void;
    };
};
