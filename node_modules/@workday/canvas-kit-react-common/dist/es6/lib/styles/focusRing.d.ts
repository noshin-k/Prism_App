import { CSSObject } from '@emotion/core';
import { CanvasTheme } from '@workday/canvas-kit-labs-react-core';
declare type FocusRingParams = {
    ringWidth: number;
    separationWidth: number;
    animate: boolean;
    inset: boolean;
    innerShadowColor: string;
    outerShadowColor: string;
};
declare function calculateFocusRing({ ringWidth, separationWidth, animate, inset, innerShadowColor, outerShadowColor, }: FocusRingParams): CSSObject;
export declare const memoizedFocusRing: typeof calculateFocusRing & import("lodash").MemoizedFunction;
export default function focusRing(ringWidth?: number, separationWidth?: number, animate?: boolean, inset?: boolean, innerShadowColor?: string, outerShadowColor?: string, memoizeCalculation?: boolean): CSSObject;
interface ThemedFocusRingOptions {
    width?: number;
    separation?: number;
    animate?: boolean;
    inset?: boolean;
    innerColor?: string;
    outerColor?: string;
    memoize?: boolean;
}
export declare function themedFocusRing(theme: CanvasTheme, options?: ThemedFocusRingOptions): CSSObject;
export {};
